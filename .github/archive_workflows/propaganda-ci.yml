name: Propaganda service CI

on:
  push:
    branches: [ main ]
    paths:
    - "backend/propaganda/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-propaganda-${{ hashFiles('backend/propaganda/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-propaganda-

    - name: Install dependencies
      working-directory: backend/propaganda
      run: pip install -r requirements.txt
    
    - name: Run Unit Tests
      working-directory: backend/propaganda
      env:
        PYTHONPATH: backend/propaganda
      run: |
        pip install pytest pytest-cov pytest-asyncio
        PYTHONPATH=$(pwd) pytest